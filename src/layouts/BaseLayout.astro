---
import '../styles/global.css';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
import StylesPanel from '../components/inspector/StylesPanel.tsx';

interface Props {
  title?: string;
  description?: string;
  image?: string;
  noindex?: boolean;
}

const {
  title = 'Ivan Uruchurtu',
  description = 'Product Designer specializing in developer tools, B2B platforms, and custom mechanical keyboards.',
  image = '/og-image-v3.png',
  noindex = false
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const ogImageURL = new URL(image, Astro.site);
---

<!DOCTYPE html>
<html lang="en" style="color-scheme: dark;">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="author" content="Ivan Uruchurtu" />
    {noindex && <meta name="robots" content="noindex, nofollow" />}
    <link rel="canonical" href={canonicalURL} />
    <link rel="icon" href="/favicon.ico" sizes="32x32">
    <link rel="icon" href="/favicon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/apple-touch-icon.png">
    <meta name="theme-color" content="#0c0c0c" />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImageURL} />
    <meta property="og:site_name" content="Ivan Uruchurtu" />
    <meta property="og:locale" content="en_US" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageURL} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Hanken+Grotesk:wght@300;400;600&family=Fraunces:ital,opsz,wght,SOFT,WONK@0,9..144,100..900,0..100,0..1;1,9..144,100..900,0..100,0..1&display=swap" rel="stylesheet" />
    <title>{title}</title>

    <!-- Structured Data -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "Person",
      "name": "Ivan Uruchurtu",
      "url": "https://ivan.cv",
      "jobTitle": "Product Designer",
      "description": "Product Designer specializing in developer tools, B2B platforms, and custom mechanical keyboards.",
      "knowsAbout": ["Product Design", "Developer Tools", "B2B Platforms", "Design Systems", "Mechanical Keyboards"],
      "sameAs": []
    })} />

    <!-- Analytics -->
    <script defer src="https://analytics.oneir.sh/script.js" data-website-id="6a8f243b-b0a6-4248-811a-fb83d5da17f9"></script>
  </head>
  <body class="flex flex-col min-h-screen">
    <div class="px-4 sm:px-6 max-w-5xl mx-auto w-full flex flex-col min-h-screen">
      <Nav />
      <main class="flex-1">
        <slot />
      </main>
      <Footer />
    </div>

    <!-- Design Inspector -->
    <StylesPanel client:load />
    <script>
      import '../scripts/animations';
      import '../scripts/inspector';
      import '../scripts/inspector-demo';
      import '../scripts/zoom';
    </script>

  </body>
</html>
